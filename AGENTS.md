**Execution Separation:** Separate architectural reasoning from direct execution. For complex tasks, propose the strategy plan and wait for my explicit approval before writing the actual code.
**Architecture & Style:** Favor DRY, KISS (simple, single-purpose pure functions), and YAGNI. Check for existing logic before writing new code. No mutating inputs or global state. Use OOP classes only for external connectors/interfaces. English comments only. All imports at the top.
**Typing:** Enforce strict typing everywhere (returns, variables, collections, complex structures). Avoid generics and default parameter values (make all parameters explicit).
**Error Handling & Logging:** Raise explicit, specific errors; never silently ignore or use catch-alls. No unprompted fallbacks. Fix root causes, not symptoms. Include rich debug context (request params, status codes). Use structured logging over string interpolation. For external APIs: retry with warnings, then raise the last error.
**Dependencies & Anti-Fabrication:** Manage dependencies via modern project configs (e.g., package.json), never globally. Never invent packages, libraries, APIs, or methods. Read installed dependency source code to verify existence and behavior, actively preventing slopsquatting supply chain attacks and avoiding deprecated APIs.
**Security & Testing:** Write testable code. Proactively handle security risks (e.g., input validation, sanitization). If applicable, provide or update unit tests for complex logic.
**Agentic File Ops:** You have access to read, edit (patch), and delete actions in the repo. Before refactoring, actively re-read the complete file content to avoid stale context. Ensure strict intra-file consistency. Immediately after editing, re-read the file to verify the patch was fully and accurately applied. Anticipate and resolve codebase-wide cascading impacts.
**Mechanical Overrides:** Do not batch more than 3 edits to the same file without a verification read. For files over 500 LOC, use offset/limit parameters to read in sequential chunks. Remember you only have grep, not an AST, so search exhaustively for direct calls, type references, and string literals before renaming or refactoring.
**Product Strategy:** Treat me as a solo entrepreneur. Align all decisions with the current product lifecycle stage and goals. Weigh technical choices heavily against time-to-market, UX, operational complexity, and direct business value. Push back against solutions misaligned with the current phase. Prioritize human-in-the-loop validation.
**Contextual BMAD Integration & Scope Constraint:** First, analyze my request to determine if it is applicable to the BMAD methodology. If the task aligns with structured workflows, adhere dynamically to the BMAD framework and strictly bind yourself to the specific BMAD agent skills required for that phase. If the request is a general task or outside the BMAD scope, execute it as a standard request without forcing BMAD constraints. When applying BMAD, implement only the exact feature requested for the current phase without building future-proofed abstractions or overengineering.